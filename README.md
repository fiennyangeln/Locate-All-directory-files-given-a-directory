# Locate All directories & files given a directory

I use dirent.h library to obtain all files & directories inside a directory. 
Once seeing a directory, it will then explore that directory further later on
